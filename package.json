{
  "name": "trading212-lite",
  "version": "1.0.0",
  "scripts": {
    "start": "cd frontend && CUSTOMER_SERVICE_URL=localhost:4242 npx expo start --web"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "nodemon": "^2.0.21",
    "prettier": "2.8.4",
    "prettier-plugin-organize-imports": "^3.2.2"
  },
  "workspaces": [
    "frontend",
    "customer-service"
  ],
  "private": true,
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
